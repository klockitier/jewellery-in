import { createFileRoute } from "@tanstack/react-router";
import { collections } from "~/config/collections";
import { pageHead } from "~/lib/seo";
export const Route=createFileRoute("/collections")({head:()=>pageHead({title:"Collections — Shri Madhav Jewellers",path:"/collections"}),component:Collections});
function Collections(){return <main className="bg-ivory"><section className="mx-auto max-w-7xl px-5 py-16 sm:px-8 lg:py-24"><p className="eyebrow">Curated for you</p><h1 className="mt-4 text-5xl">Our Collections</h1><div className="mt-12 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">{collections.map(c=><a href={`/collections/${c.slug}`} key={c.slug} className="group relative block h-[28rem] overflow-hidden rounded-xl"><img src={c.image} alt={c.alt} className="absolute inset-0 h-full w-full object-cover transition duration-700 group-hover:scale-105"/><div className="absolute inset-0 bg-gradient-to-t from-charcoal/90 via-charcoal/30 to-transparent"/><div className="absolute bottom-0 p-6 text-ivory"><p className="eyebrow !text-gold-bright">{c.tagline}</p><h2 className="mt-2 font-display text-3xl">{c.name}</h2><p className="mt-3 text-sm text-ivory/70">{c.description}</p></div></a>)}</div></section></main>}
