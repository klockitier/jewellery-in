import { createFileRoute } from "@tanstack/react-router"; import { useEffect,useState } from "react"; import { ordersStore,type Order } from "~/lib/orders"; import { formatINR } from "~/lib/format"; import { pageHead } from "~/lib/seo";
export const Route=createFileRoute("/account")({head:()=>pageHead({title:"Your Account — Yogesh Jewellers",path:"/account"}),component:Account});
function Account(){const [orders,setOrders]=useState<Order[]>([]); useEffect(()=>setOrders(ordersStore.list()),[]);return <main className="bg-ivory"><section className="mx-auto max-w-4xl px-5 py-16 sm:px-8 lg:py-24"><p className="eyebrow">Your account</p><h1 className="mt-4 text-5xl">Order history</h1>{!orders.length?<div className="mt-12 rounded-xl border border-line bg-cream p-10 text-center"><p className="text-lg text-muted">No orders yet — your orders will appear here</p><a className="btn btn-gold mt-7 inline-flex" href="/catalogue">Browse jewellery</a></div>:<div className="mt-10 space-y-4">{orders.map(o=><a href={`/order/${o.id}`} key={o.id} className="block rounded-xl border border-line bg-white p-5 transition hover:border-gold"><div className="flex flex-wrap justify-between gap-3"><b>{o.id}</b><b>{formatINR(o.amounts.total)}</b></div><p className="mt-2 text-sm text-muted">{new Date(o.createdAt).toLocaleDateString("en-IN")} · {o.status} · payment {o.payment.status}</p></a>)}</div>}</section></main>}
