import { createFileRoute } from "@tanstack/react-router";
import { categories } from "~/lib/catalogue";
import { JewelIcon } from "~/components/JewelIcon";
import { pageHead } from "~/lib/seo";
export const Route=createFileRoute("/categories")({head:()=>pageHead({title:"Shop by Category — Shri Madhav Jewellers",path:"/categories"}),component:Categories});
function Categories(){return <main className="bg-cream"><section className="mx-auto max-w-7xl px-5 py-16 sm:px-8 lg:py-24"><p className="eyebrow">Browse the craft</p><h1 className="mt-4 text-5xl">All Categories</h1><p className="mt-4 max-w-xl text-muted">Find the piece made for your moment, from everyday essentials to heirloom gold.</p><div className="mt-12 grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-5">{categories.map(c=><a href={`/category/${c.slug}`} key={c.slug} className="group rounded-xl border border-line bg-white p-6 text-center transition hover:-translate-y-1 hover:border-gold/60 hover:shadow-lg"><span className="mx-auto flex h-20 w-20 items-center justify-center rounded-full border border-gold/30 text-gold-deep"><JewelIcon kind={c.icon} className="h-10 w-10 transition group-hover:scale-110"/></span><h2 className="mt-4 font-display text-xl">{c.name}</h2><p className="mt-1 text-xs text-muted">{c.tagline}</p></a>)}</div></section></main>}
