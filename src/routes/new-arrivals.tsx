import { createFileRoute } from "@tanstack/react-router"; import { newArrivals } from "~/lib/catalogue"; import { ProductCard } from "~/components/ProductCard"; import { pageHead } from "~/lib/seo";
export const Route=createFileRoute("/new-arrivals")({head:()=>pageHead({title:"New Arrivals — Shri Madhav Jewellers",path:"/new-arrivals"}),component:()=> <ProductListing title="New Arrivals" eyebrow="Just in" items={newArrivals}/>});
function ProductListing({title,eyebrow,items}:{title:string;eyebrow:string;items:typeof newArrivals}){return <main className="bg-cream"><section className="mx-auto max-w-7xl px-5 py-16 sm:px-8 lg:py-24"><p className="eyebrow">{eyebrow}</p><h1 className="mt-4 text-5xl">{title}</h1><p className="mt-4 text-muted">Fresh from our karigars, crafted for now and made to last.</p><div className="mt-12 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">{items.map(p=><ProductCard key={p.id} product={p}/>)}</div></section></main>}
