import { createFileRoute } from "@tanstack/react-router"; import { siteConfig } from "~/config/site"; import { pageHead } from "~/lib/seo";
export const Route=createFileRoute("/about")({head:()=>pageHead({title:"About Us — Yogesh Jewellers",path:"/about"}),component:About});
function About(){return <main className="bg-ivory"><section className="mx-auto max-w-4xl px-5 py-16 sm:px-8 lg:py-24"><p className="eyebrow">Our showroom · {siteConfig.estd}</p><h1 className="mt-4 text-5xl">Crafted with devotion, trusted across generations.</h1><p className="mt-7 text-lg leading-relaxed text-muted">Walk into our showroom for a personal consultation — try the pieces, meet the craftsmen, and take home jewellery that’s weighed, hallmarked and billed transparently in front of you.</p><div className="mt-12 grid gap-6 sm:grid-cols-2"><div className="card-luxe rounded-xl p-7"><h2 className="font-display text-2xl">Our promise</h2><p className="mt-3 leading-relaxed text-muted">Every piece is selected for its finish and purity, with live metal rates, clear making charges and BIS-hallmarked gold.</p></div><div className="card-luxe rounded-xl p-7"><h2 className="font-display text-2xl">Visit us</h2><p className="mt-3 text-muted">{siteConfig.address.line1}, {siteConfig.address.line2}, {siteConfig.address.city}, {siteConfig.address.state} — {siteConfig.address.pincode}</p>{siteConfig.hours.map(h=><p key={h.days} className="mt-2 text-sm text-muted"><b className="text-ink">{h.days}:</b> {h.hours}</p>)}</div></div><a href="/contact" className="btn btn-gold mt-10 inline-flex">Get in touch</a></section></main>}
