import { createFileRoute } from "@tanstack/react-router"; import { siteConfig } from "~/config/site"; import { pageHead } from "~/lib/seo";
export const Route=createFileRoute("/contact")({head:()=>pageHead({title:"Contact — Shri Madhav Jewellers",path:"/contact"}),component:Contact});
function Contact(){return <main className="bg-cream"><section className="mx-auto max-w-5xl px-5 py-16 sm:px-8 lg:py-24"><p className="eyebrow">We’re here to help</p><h1 className="mt-4 text-5xl">Contact the showroom</h1><div className="mt-12 grid gap-6 md:grid-cols-2"><div className="card-luxe rounded-xl p-7"><h2 className="font-display text-2xl">Talk to us</h2><a className="mt-5 block text-gold-deep" href={`tel:${siteConfig.phone}`}>{siteConfig.phoneDisplay}</a><a className="mt-3 block text-gold-deep" href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a><p className="mt-6 text-sm text-muted">For a quick response, call during showroom hours. Email links open your own mail app; no email form is connected yet.</p><a href={`https://wa.me/${siteConfig.phone.replace(/\D/g,"")}`} className="btn btn-gold mt-7 inline-flex" target="_blank" rel="noreferrer">WhatsApp us</a></div><div className="card-luxe rounded-xl p-7"><h2 className="font-display text-2xl">Find us</h2><p className="mt-5 leading-relaxed text-muted">{siteConfig.address.line1}, {siteConfig.address.line2}<br/>{siteConfig.address.city}, {siteConfig.address.state} — {siteConfig.address.pincode}, {siteConfig.address.country}</p>{siteConfig.hours.map(h=><p key={h.days} className="mt-3 text-sm text-muted"><b className="text-ink">{h.days}:</b> {h.hours}</p>)}<a href="https://maps.google.com/?q=Shri+Madhav+Jewellers" target="_blank" rel="noreferrer" className="btn btn-outline mt-7 inline-flex">Get directions</a></div></div></section></main>}
